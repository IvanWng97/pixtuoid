[advisories]
# Each ignore is Linux-only build-graph exposure with no untrusted input; a
# tracking issue owns removal when upstream releases (see the issue for the
# full dependency-path trace).
ignore = [
    # quick-xml 0.39 pair, reachable only via wayland-scanner (build-time
    # codegen over vendored Wayland protocol XML — never attacker input).
    # Fix needs wayland-rs to release against quick-xml >=0.41.
    "RUSTSEC-2026-0194",
    "RUSTSEC-2026-0195",
    # ttf-parser declared unmaintained; only via sctk-adwaita (Linux Wayland
    # CSD titlebar text, local system fonts). Upstream is migrating to skrifa.
    "RUSTSEC-2026-0192",
]

[licenses]
allow = [
    "MIT",
    "Apache-2.0",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "CC0-1.0",
    "ISC",
    "Unicode-3.0",
    "Unicode-DFS-2016",
    "Zlib",
]

[bans]
multiple-versions = "warn"
wildcards = "allow"

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []
