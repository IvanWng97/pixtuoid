{
  "origin": "composed",
  "note": "cwd /Users/dev/cwproj, and every payload carries only event+cwd. CANNOT be recorded by capture-fixture: codewhale is the one env-mode source \u2014 the shim reads DEEPSEEK_* env vars and never touches stdin (pixtuoid-hook/src/main.rs), so a stdin tee sees nothing \u2014 and headless `codewhale exec` fires no hooks at all (source/codewhale.rs). What a fixture can pin here is the SHIM's synthesized envelope, which is what this is."
}
