{
  "origin": "recorded",
  "cli": "codewhale",
  "version": "0.9.7 (5e3ac84c5cb9)",
  "captured": "2026-08-15",
  "command": "codewhale (interactive TUI — `exec` fires no hooks and `run` is the TUI launcher)",
  "note": "`captured` was 2026-08-14 (the shell recorder dated in LOCAL time while the shim stamped UTC); corrected to 2026-08-15 from the payloads' own `_shim_ts_ms`."
}
