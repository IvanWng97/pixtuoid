{
  "origin": "composed",
  "note": "cwd /Users/dev/cwproj, and every payload carries only event+cwd. Recordable, but only from an INTERACTIVE TUI turn: headless `codewhale exec` fires no hooks at all (source/codewhale.rs), and `codewhale run` is the TUI launcher rather than a second headless path. Drive it by hand the way kimi/permission-flow was: `just capture-fixture codewhale tool-run \"$SHELL\"`."
}
