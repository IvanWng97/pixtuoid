{
  "captured": "2026-08-16",
  "cli": "hermes",
  "command": "scripts/lib/tuidrive.py driving `hermes chat`, prompt asking for a `rm -rf` the approval system gates",
  "origin": "recorded",
  "prompt": "Run exactly this shell command and nothing else: rm -rf /tmp/pixtuoid-probe",
  "version": "Hermes Agent v0.20.1 (2026.8.13)",
  "note": "The gate fires TWICE for one command (both `pre_approval_request`, same `extra`), which is why a re-notified Waiting must not clear the gate the first one set. `tool_name`/`tool_input` are null; the WHY rides `extra.description` = \"delete in root path\". Verbatim: the recorder's generic sandbox cwd meant nothing needed redacting."
}
