{
  "origin": "recorded",
  "cli": "claude",
  "version": "2.1.233 (Claude Code)",
  "captured": "2026-08-15",
  "command": "claude --permission-mode auto (interactive TUI, driven under a pty), prompted to dispatch a Task subagent",
  "note": "The CHILD's own transcript, which is where `attributionAgent` lives — the parent's had zero. Its hooks are deliberately NOT shipped here: CC's hook transcript_path always points at the PARENT, so hooks and this transcript key to different sessions and could not share a one-AgentId scenario. Home path redacted to /Users/dev."
}
