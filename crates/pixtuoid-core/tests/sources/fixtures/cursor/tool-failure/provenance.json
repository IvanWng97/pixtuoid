{
  "origin": "recorded",
  "cli": "cursor-agent",
  "version": "2026.09.18-9a7762b",
  "captured": "2026-09-19",
  "command": "cursor-agent -p --trust --model auto {prompt}",
  "prompt": "Read the file does-not-exist.txt in this directory and tell me exactly what happened.",
  "deidentified": {
    "method": "decoder-allowlist",
    "blanked": 122
  },
  "note": "Recorded with `--model auto`: cursor-agent on a free plan refuses named models. Cursor invokes its hook command several times per event and only one copy keeps the env prefix its install writes; the unstamped copies are what `source/cursor.rs`'s cross-fire guard drops."
}
