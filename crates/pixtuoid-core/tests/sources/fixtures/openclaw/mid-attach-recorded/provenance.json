{
  "origin": "recorded",
  "cli": "openclaw",
  "version": "2026.7.1 (2d2ddc4)",
  "captured": "2026-08-15",
  "command": "gateway started BEFORE the recorder (its gateway_start hook exits 0 into a socket that does not exist yet), then one `openclaw tui` turn inside the capture window",
  "note": "The mid-attach shape from real bytes: session_start / before_agent_run / agent_end with NO gateway_start, which is what pixtuoid sees when it attaches to a gateway that was already running. The FAILED arm is not here — `--timeout-ms 1` did not fail the run (agent_end came back success:true), so the composed gateway_mid_attach_failed_run keeps that one field."
}
