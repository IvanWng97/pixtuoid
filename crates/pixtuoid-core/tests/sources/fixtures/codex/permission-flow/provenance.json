{
  "origin": "composed",
  "note": "Pins the escalation shape `function_call_needs_approval` reads: a `function_call` whose `arguments` carry \"sandbox_permissions\": \"require_escalated\". NOT legacy — codex 0.147.0's own system prompt still instructs the model to use exactly this parameter (verbatim in a 2026-08-14 rollout), and a census of 104 local rollouts / 8323 function_call records found 47 real escalated calls across 10 of them. The recorded sibling permission-recorded took codex's OTHER tool surface — a `custom_tool_call` named `exec` with a JS `input` and no escalation marker, where the gate exists only on the hook wire. Which surface a turn uses is model-chosen, which is why one capture is not evidence that the other is dead."
}
