{
  "captured": "2026-09-10",
  "cli": "codex",
  "command": "codex exec {prompt}",
  "origin": "recorded",
  "version": "codex-cli 0.153.4",
  "note": "Redacted: the `world_state` line (the installed skill roster with paths plus the host timezone); `world_state` is in codex's ignored set, so dropping it costs no evidence. Redacted: the recorder's own MCP server / skill inventory (tool NAMES only, no credentials) to generic placeholders \u2014 a public repo and an immutable crates.io tarball are the wrong home for one machine's tooling roster, and no decoder reads those fields. Home path redacted to /Users/dev; the owner column of a captured `ls -la` to dev. Redacted: the `AGENTS.md instructions` attachment \u2014 the recorder's own `~/.codex/AGENTS.md`, new at this version, carrying its mem0 rules and identity \u2014 to a placeholder, on the same reasoning as the skill inventory above. Redacted: the recorder's installed plugin roster \u2014 skill and agent names, their descriptions, and the install paths that give the names back \u2014 to `example-skill-N` / `example-plugin-N:example-agent` placeholders, collapsed to three entries because the COUNT is this machine's inventory size too. Each CLI's own bundled set is kept, told apart mechanically rather than by a name list: a `:` in a claude-code agent type means plugin-provided, copilot tags `<location>builtin</location>`, codex roots its own at `r2/` or `.codex/skills/.system/`. No decoder reads any of these fields and every conformance golden is byte-identical."
}
