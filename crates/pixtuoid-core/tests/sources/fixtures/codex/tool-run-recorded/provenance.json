{
  "origin": "recorded",
  "cli": "codex",
  "version": "codex-cli 0.147.0",
  "captured": "2026-08-14",
  "command": "codex exec {prompt}",
  "note": "Redacted: the `world_state` line, 26.8 KB of it, listed 57 installed skill names with their paths plus the host timezone. `world_state` is in codex's ignored set, so dropping the line left the golden snapshot byte-identical \u2014 which is how the redaction was shown to cost no evidence. Redacted: the recorder's own MCP server / skill inventory (tool NAMES only, no credentials) to generic placeholders \u2014 a public repo and an immutable crates.io tarball are the wrong home for one machine's tooling roster, and no decoder reads those fields."
}
