{
  "origin": "recorded",
  "cli": "opencode",
  "version": "1.18.30",
  "captured": "2026-09-19",
  "command": "scripts/lib/capture-opencode-session.sh — opencode serve, then POST /session and DELETE /session/<id> against it",
  "deidentified": {
    "method": "decoder-allowlist",
    "blanked": 15
  },
  "note": "`session.deleted` has exactly one door, a mutation THROUGH a running server — the driver script says why. Costs no model turn: a session's creation and deletion are the whole scenario."
}
