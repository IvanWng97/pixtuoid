{
  "origin": "recorded",
  "cli": "grok",
  "version": "unknown",
  "captured": "2026-08-15",
  "command": "grok --permission-mode auto (interactive TUI, driven under a pty), prompted to spawn_subagent with run_in_background false",
  "note": "The PARENT's transcript, where the blocking spawn lives — `background: false` on the wire, which is the ONLY case that mints ToolDetail::Task (a background spawn posts at dispatch and would let the b1 drain-cascade evict the live child). The child runs as its own grok session with its own transcript, so the two cannot share a one-AgentId scenario; the hook half of the same run lives in tests/sources/grok/fixtures. Home path redacted to /Users/dev."
}
