{
  "captured": "2026-08-31",
  "cli": "omp",
  "command": "omp (interactive TUI, driven by scripts/lib/capture-omp-tui.py exit \u2014 /exit before any prompt)",
  "note": "hook sessionFile home path redacted to /Users/dev; bytes otherwise verbatim. interactive TUI under a pty, /exit before any prompt: omp persists a HEADER (the /exit keystrokes count as a composer draft) but never a session_exit, so the bridge shutdown is the only end signal",
  "origin": "recorded",
  "version": "omp/18.0.11"
}
