{
  "captured": "2026-08-31",
  "cli": "omp",
  "command": "omp (interactive TUI, driven by scripts/lib/capture-omp-tui.py exit — /exit before any prompt)",
  "note": "hook sessionFile home path redacted to /Users/dev. interactive TUI under a pty, /exit before any prompt: omp persists a HEADER (the /exit keystrokes count as a composer draft) but never a session_exit, so the bridge shutdown is the only end signal",
  "origin": "recorded",
  "version": "omp/18.0.11",
  "deidentified": {
    "method": "decoder-allowlist",
    "blanked": 10
  }
}
