{
  "origin": "recorded",
  "cli": "claude",
  "version": "unknown",
  "captured": "unknown",
  "command": "see the module header",
  "note": "REAL SubagentStart/Stop wire payloads, sanitized to synthetic ids and a generic cwd; `last_assistant_message`/`background_tasks` truncated but KEPT so the decoder's tolerance of fields we don't consume stays pinned. Predates the socket recorder, so it carries no `_shim_ts_ms`/`_pid` stamps — that dates it, it does not make it composed."
}
