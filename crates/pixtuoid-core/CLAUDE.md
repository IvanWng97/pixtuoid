# pixtuoid-core — agent guide

The **headless library**: no terminal dependencies (`ratatui`/`crossterm`/`stdout`
are forbidden here — see workspace invariant #1). Owns the source/decoder seam,
the reducer/state machine, sprite parsing, and the shared grid/walkability
vocabulary. The sim geometry (layout, pose derivation, walk physics) lives in
`pixtuoid-scene` — the engine owns its geometry; only the coherence-bound
`walkable.rs` stays here (see [`SHARP-EDGES.md`](SHARP-EDGES.md)). The scene engine (`pixtuoid-scene`)
and the binary (`pixtuoid`) sit on top of this. See the workspace
[`CLAUDE.md`](../../CLAUDE.md) for cross-cutting rules.

## Layout

Annotated tree in [`LAYOUT.md`](LAYOUT.md) — grep it for a filename.

<!-- layout:start · generated from LAYOUT.md by `just gen-guides` — edit the tree there, not this skeleton -->
```
src/
├── source/             Source trait, hook+jsonl decoders, listeners …
├── state/              SceneState + Reducer (event coordinator: Transport-tagged …
├── sprite/             .sprite parser, pack.toml loader, blit_frame blitter …
├── platform.rs         cross-platform home-dir resolution (user_home() …
├── grid.rs             Grid<T> — a width×height row-major Vec<T> with checked …
├── harness.rs          `harness` FEATURE (non-default, dev-only — absent from the …
├── id.rs               AgentId + from_parts/from_transcript_path (moved out of …
├── walkable.rs         WalkableMask = Grid<bool> (static obstacle mask) + …
└── tests/              one integration test per concern
```

- **Burn-tier plumbing (model flame):** `AgentEvent::ModelInfo { model, effort }` carries RAW wire strings (interpret-at-paint — the …
- **Token-meter plumbing (#632, the desk paper tower):** `AgentEvent::Usage { fresh_tokens }` carries a per-reading FRESH-spend delta (new input + cache …
- **Focus-jump plumbing (#focus-jump):** the shim fills `_pid` (getppid) into the hook envelope WHEN ABSENT — opencode's plugin and …
<!-- layout:end -->

## Known sharp edges (don't be surprised by these)

Full entries in [`SHARP-EDGES.md`](SHARP-EDGES.md) — grep it for the phrase.

<!-- edges:start · generated from SHARP-EDGES.md by `just gen-guides` — edit the entry there, not this line -->
- **There is NO core `render` module and no core render trait.** `blit_frame` (the scene painter's primitive) is the only pre-scene-split render helper that …
- **`walkable.rs` is coherence-bound to this crate — it did NOT move with the sim-geometry cluster.** When layout/pose/physics moved to `pixtuoid-scene` (the engine owns its geometry) …
- **A daemon's runtime identity is the SOURCE's own wire fact, and for OpenClaw that is the resolved gateway PORT — not the profile, not the pid.** `SceneState::daemons` is keyed `(source, DaemonInstanceId)` because OpenClaw officially supports …
- **`GatewayDown` CREATES an absent instance; `PidExited` never does — the asymmetry is the point, not a hole in the creation-polarity rule.** Both look like "death" deltas, so a review will ask why only one is non-creating. The difference …
- **A `gatewayPort`-LESS OpenClaw envelope falls back to ONE documented legacy instance instead of being rejected — deliberately.** Installing the plugin is a one-shot `connect`; upgrading the pixtuoid binary does NOT re-render …
- **The two pid→fan-out shells (`HookPidWatch` in `hook/pid_watch.rs`, the daemon's `PresenceExitWatch` in `daemon/native.rs`) stay SEPARATE — do NOT hoist a generic `PidFanout<K>`.** The DEEP primitive is ALREADY shared at the right seam: `source/exit_watch.rs::ExitWatch` …
- **`HookPidWatch` arms the exit watch on the SECOND sighting of a `(pid, agent)` pair, never the first — the one-event lag IS the guard, not an oversight.** The envelope's `_pid` is the shim's best guess at which ancestor is the agent CLI, and a runner …
- **The `native` (default) feature gates the ASYNC SOURCE RUNTIME, not the decoders — and the gates are MODULE-level, not item scatter.** `default-features = false` drops tokio/notify and everything behind …
- **The per-CLI home resolvers MIRROR each CLI's own; the axes deliberately NOT mirrored are listed here** (#880). A resolver shaped `$SOME_ENV else ~/.<cli>` is correct only if someone READ that CLI's …
- **CC hook payloads DO include `tool_use_id`** in `PreToolUse` and `PostToolUse` (verified by sniffing live payloads). The decoder reads it …
- **That hook-wins dedup is ONE-directional — a JSONL-first tool inflates `tool_call_count` by 1 (cosmetic).** The dedup drops a JSONL event only when a matching hook `tool_use_id` was ALREADY recorded; the …
- **The `active_tasks` insert in `track_active_tasks` is not slot-gated, and that's HARMLESS — don't "fix" it with a `contains_key` guard.** Unlike the hook-wins dedup RECORD (which gates on `scene.agents.contains_key`), the insert runs …
- **The JSONL watcher's DECODED-event send is blockable, unlike the hook path's `CONN_TIMEOUT` — a deliberate asymmetry.** `walk_jsonl` awaits `tx.send(..)` on the bounded (`EVENT_CHANNEL_CAPACITY` = 256) reducer …
- **CC now keys on the session UUID, not the transcript path.** The hook uses `IdKey::SessionId` (the `session_id` field); the watcher overrides `IdDeriver` …
- **CC hook `transcript_path` always points to the PARENT'S transcript**, even when a subagent is the actor — so subagent TOOL hook events hash to the parent's …
- **JSONL watcher skips historical transcripts — on EVERY first-sight path, not just startup.** `walk_jsonl` in `source/jsonl/walk.rs` runs ONE first-sight gate (`should_seed_at_eof`) the …
- **Watch backend: native in prod, polling in tests.** `JsonlWatcher::run` watches via `notify`'s `RecommendedWatcher` (FSEvents/inotify/…) plus the …
- **A hook event from an UNKNOWN session id REGISTERS it — hooks are proof of life.** A hook can only come from a live process, so a Hook-transport …
- **Agent removal needs a `SessionEnd`; abrupt exits have none and fall back to the slow stale-sweep.** A slot is GC'd `EXIT_GRACE_WINDOW` (4.5 s) after `exiting_at` is set, which only an …
- **A subagent registered AFTER its parent's cascade escapes it, and is reaped only by its OWN stale sweep — deliberate, because a parent's `exiting_at` is NOT a terminal verdict.** `cascade_exit` is a one-shot PUSH at the moment the parent exits; `register_slot` …
- **Resurrect-in-place starts from clean correlation state.** A `SessionStart` on an EXITING root slot cancels the walkout (`fsm::resurrect_in_place`) AND …
- **Codex subagents (`spawn_agent`) are wired via the `SubagentStart`/`SubagentStop` HOOKS, not JSONL paths.** Unlike CC (whose subagent has a `<parent>/subagents/agent-*.jsonl` transcript → …
- **Subagent display names come from `attributionAgent` in JSONL.** The decoder strips the plugin prefix (`feature-dev:code-explorer` → `code-explorer`) and emits …
- **`AgentSlot.state_started_at` is `std::time::SystemTime`** — process-local in practice (no wall-clock anchoring). The pose system computes elapsed time …
- **`ActivityState::Active` ≠ "tool is currently executing".** CC fires PreToolUse → PostToolUse around every individual tool call, so without debouncing the …
- **The reducer's permission `Waiting` resolves on the gated tool's PostToolUse.** A `PermissionRequest`/decision sets `Waiting`; the matching tool's `ActivityEnd` (PostToolUse) …
<!-- edges:end -->

## Where to look

Answers live in [`WHERE-TO-LOOK.md`](WHERE-TO-LOOK.md), so a session
pays for the entry it needs instead of all of them. Grep it for the
question:

<!-- lookup:start · generated from WHERE-TO-LOOK.md by `just gen-guides` — edit the entry there, not this list -->
- How does the per-agent state machine work?
- Why is the subagent's sprite the right one and not the parent?
- How does multi-source decoding work?
- Why don't old idle sessions show on startup?
<!-- lookup:end -->

## Keeping the decode mapping current (upstream drift)

Moved to [`UPSTREAM-DRIFT.md`](UPSTREAM-DRIFT.md) — read it before touching this area.

## When refactoring

The channel type, `Source` trait, `AgentEvent` enum, and reducer signature are workspace-wide contracts — see the root [`CLAUDE.md`](../../CLAUDE.md) "When refactoring" for the full list of test files to update and the add-a-CLI checklist.
