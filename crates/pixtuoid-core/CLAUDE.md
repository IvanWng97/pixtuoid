# pixtuoid-core — agent guide

The **headless library**: no terminal dependencies (`ratatui`/`crossterm`/`stdout`
are forbidden here — see workspace invariant #1). Owns the source/decoder seam,
the reducer/state machine, sprite parsing, and the shared grid/walkability
vocabulary. The sim geometry (layout, pose derivation, walk physics) lives in
`pixtuoid-scene` — the engine owns its geometry; only the coherence-bound
`walkable.rs` stays here (see [`SHARP-EDGES.md`](SHARP-EDGES.md)). The scene engine (`pixtuoid-scene`)
and the binary (`pixtuoid`) sit on top of this. See the workspace
[`CLAUDE.md`](../../CLAUDE.md) for cross-cutting rules.

## Layout

Module map: `ls src/` — each file's `//!` header is its annotation. The
cross-cutting plumbing digests (burn tier, token meter, focus-jump) live in
[`WHERE-TO-LOOK.md`](WHERE-TO-LOOK.md).

## Known sharp edges (don't be surprised by these)

Full entries in [`SHARP-EDGES.md`](SHARP-EDGES.md) — grep it for the phrase.

<!-- edges:start · generated from SHARP-EDGES.md by `just gen-guides` — edit the entry there, not this line -->
- **There is NO core `render` module and no core render trait.** `blit_frame` is the only pre-scene-split render helper that stays. New render targets are thin …
- **`walkable.rs` is coherence-bound to this crate — it did NOT move with the sim-geometry cluster.** `WalkableMask` is a type alias for `Grid<bool>` whose obstacle ops are an inherent …
- **A daemon's runtime identity is the SOURCE's own wire fact, and for OpenClaw that is the resolved gateway PORT — not the profile, not the pid.** The profile is install-scope, never on the hook wire, and profile↔gateway is 1:N (verified …
- **`GatewayDown` CREATES an absent instance; `PidExited` never does — the asymmetry is provenance, not a hole.** `gateway_stop` is a first-hand wire report from a demonstrably-alive process, so materializing …
- **`SessionEnded` alone among the presence arms does NOT resurrect a Down gateway — don't harmonize it with its siblings.** The recorded clean shutdown is `gateway_stop` then `session_end` 2 ms later …
- **A `gatewayPort`-LESS OpenClaw envelope falls back to ONE documented legacy instance instead of being rejected — deliberately.** Upgrading the pixtuoid binary does not re-render the installed plugin, so old plugins stamp no …
- **The two pid→fan-out shells (`HookPidWatch`, the daemon's `PresenceExitWatch`) stay SEPARATE — do NOT hoist a generic `PidFanout<K>`.** The deep primitive is already shared at the right seam (`source/exit_watch.rs::ExitWatch`); each …
- **`HookPidWatch` arms the exit watch only when one agent reports the SAME pid twice IN A ROW — the one-event lag IS the guard.** A runner that wraps every hook in a per-invocation shell makes `_pid` a corpse-to-be; acting on …
- **The `native` (default) feature gates the ASYNC SOURCE RUNTIME, not the decoders — and the gates are MODULE-level, not item scatter.** `default-features = false` drops tokio/notify and every runtime half (`hook/`, `jsonl/` …
- **The per-CLI home resolvers MIRROR each CLI's own; the axes deliberately NOT mirrored are listed here** (#880). Where a source tails a transcript, an unmirrored axis is fail-silent (empty office, no …
- **The hook-wins dedup is asymmetric BY EVENT KIND (#150).** A recorded hook End suppresses both JSONL kinds (covers the tool's lagged pair), but a recorded …
- **That dedup is also ONE-directional — a JSONL-first tool inflates `tool_call_count` by 1, cosmetically.** Only the hook arm writes `recent_hook_tool_uses`, so when JSONL wins the two-task delivery race …
- **The `active_tasks` insert in `track_active_tasks` is not slot-gated, and that's HARMLESS — don't add a `contains_key` guard.** A desk-exhaustion orphan entry eats nothing (every reader is slot-guarded) and `tick`'s retain …
- **The JSONL watcher's DECODED-event send is blockable, unlike the hook path's `CONN_TIMEOUT` — a deliberate asymmetry.** Dropping a decoded transcript delta would desync the per-file cursor's tail-follow (which …
- **CC keys on the session UUID, not the transcript path.** Hook `IdKey::SessionId` == watcher `cc_id_from_path` (filename stem) == `detect_parent_id`'s …
- **CC hook `transcript_path` always points to the PARENT'S transcript**, even when a subagent acts — so subagent tool hooks hash to the parent, and the reducer …
- **The subagent-dispatch tool is detected SEMANTICALLY — by `subagent_type` in the input — with the name `Agent` only as fallback.** CC renamed the tool `Task`→`Agent` undocumented (v2.1.63, issue #29677), which silently broke …
- **Liveness flows UP the subagent tree** (`scope::refresh_lineage`): any activity refreshes `last_event_at` for the actor and every …
- **The JSONL first-sight gate runs on EVERY path a file is first seen — seed, 250ms rescan, 60s poll, notify — not just startup (#85).** `walk_jsonl::should_seed_at_eof`: outside `DEFAULT_INITIAL_WINDOW` (1h) OR carrying an end …
- **"Recent" is the source's own ACTIVITY clock where it has one, not the file mtime.** CC writes session-metadata sidecars into OTHER, dead sessions' transcripts, bumping mtime …
- **A liveness-probe vouch bypasses the gate's RECENCY half, NEVER its terminator half.** A vouch answers "is the owning process alive", not "is this session over" (omp's fd vouch fires …
- **The exit ladder is instant (ms) > negative vouch (~60–120s) > ProofOfLife TTL + stale sweeps — and a probe FAILURE changes nothing.** `LivenessProbe` returns `Option<ProbeSnapshot>`: `None` = enumeration failed, watcher freezes …
- **An OVERSIZED pending span (>1 MiB) is skipped to EOF, never replayed — but it still registers (#204) and rescues in-flight Task dispatches (#222).** The tail 256 KiB is scanned for UNMATCHED Task dispatches and exactly those re-emit …
- **Watch backend: native in prod, polling in tests — don't wire real FSEvents into tests.** A real FSEvents stream costs tens of seconds per test to set up …
- **A hook event from an UNKNOWN session id REGISTERS it — hooks are proof of life; JSONL events for unknown ids stay no-ops.** A hook only comes from a live process, and a permission-parked session appends nothing to its …
- **A refused (desk-exhausted) hook registration must NOT record its `tool_use_id`** — the stale record would dedup-eat the JSONL `ActivityStart` that finally registers the session …
- **A child's end is remembered by tombstone (5s) + child ledger (#244/#246) — and the ledger rides TWO deliberately different clocks.** The 5s `SessionEnd` tombstone gates only unknown-id reorder skew; the reducer-private ledger …
- **CC has no durable JSONL exit record — and user-controllable content must never drive lifecycle.** Modern CC persists no `/exit` marker, and the old content-based `<command-name>` matcher …
- **Codex Idle reaps at 5 min (`STALE_SHORT_IDLE_TIMEOUT`); CC keeps 30 — don't add a short CC reaper.** The policy is registry-derived (`SourceCaps::short_idle_reap()` = …
- **Two guards stop a stale-swept parent's cascade from evicting a LIVE subagent** (k8s liveness-vs-readiness): liveness up-propagation (`refresh_lineage`) keeps ancestors of a …
- **The b1 Task-drain cascade is grace-DEFERRED (2.5s, #151), and the chained-dispatch linger is unbounded — don't restore immediacy.** A parallel second dispatch is only visible via its lagged JSONL copy; an immediate cascade at …
- **A subagent registered AFTER its parent's cascade escapes it — deliberate, because a parent's `exiting_at` is NOT a terminal verdict.** Both obvious fixes were adjudicated against: REFUSING the registration breaks the …
- **Resurrect-in-place starts from clean correlation state.** A `SessionStart` on an EXITING root cancels the walkout AND evicts the dead life's …
- **Codex subagents are wired via the `SubagentStart`/`SubagentStop` HOOKS, not JSONL paths.** A Codex child's rollout is FLAT under `sessions/`, so the hooks are the only carrier of the …
- **Subagent display names come from `attributionAgent` in JSONL** (plugin prefix stripped → `AgentEvent::Rename`); parents get their cwd basename, and …
- **`AgentSlot.state_started_at` is process-local `SystemTime`, and `SceneState`'s serde derive is NOT a wire contract.** The pose system computes elapsed time against it — don't wall-clock-anchor it. The …
- **`ActivityState::Active` ≠ "tool is currently executing".** PreToolUse→PostToolUse fire around every call, so `ActivityEnd` only ARMS pending-idle; `tick` …
- **grok leader-mode sessions are DELIBERATELY UNVOUCHED (#826) — the #638 socket vouch was deleted, not repaired.** `--leader` runs the agent in a shared process while `active_sessions.json` records only each TUI …
- **grok fires OTHER CLIs' hooks with GROK envelopes — the cross-fire guard drops them QUIETLY.** grok scans `~/.claude/settings.json` and `~/.cursor/hooks.json` by default and executes those …
- **grok's `spawn_subagent` defaults `background=true`, where `post_tool_use` fires at SPAWN — so `ToolDetail::Task` is minted ONLY for explicit `background:false`.** Treating a background spawn as a Task would let the b1 drain-cascade evict the LIVE child ~2.5s …
- **The permission `Waiting` resolves on the gated tool's PostToolUse — and the gate (`gated_before_waiting`) has FOUR clear sites whose conditionality is load-bearing.** The two eviction sites (`tick` retain, `sweep_exited` remove); the Task-drain clear, CONDITIONAL …
<!-- edges:end -->

## Where to look

Answers live in [`WHERE-TO-LOOK.md`](WHERE-TO-LOOK.md), so a session
pays for the entry it needs instead of all of them. Grep it for the
question:

<!-- lookup:start · generated from WHERE-TO-LOOK.md by `just gen-guides` — edit the entry there, not this list -->
- How does the per-agent state machine work?
- Why is the subagent's sprite the right one and not the parent?
- How does multi-source decoding work?
- How does the model flame (burn tier) get its data?
- How does the token meter (the desk paper tower) get its data?
- How does focus-jump resolve the agent's pid?
- Why don't old idle sessions show on startup?
<!-- lookup:end -->

## Keeping the decode mapping current (upstream drift)

Moved to [`UPSTREAM-DRIFT.md`](UPSTREAM-DRIFT.md) — read it before touching this area.

## When refactoring

The channel type, `Source` trait, `AgentEvent` enum, and reducer signature are workspace-wide contracts — see the root [`CLAUDE.md`](../../CLAUDE.md) "When refactoring" for the full list of test files to update and the add-a-CLI checklist.
