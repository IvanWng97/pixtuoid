name: 'Setup Cargo & Just'
description: 'Sets up Rust toolchain, rust-cache, and just'

inputs:
  components:
    description: 'Comma-separated list of toolchain components'
    required: false
    default: ''
  targets:
    description: 'Comma-separated list of target triples'
    required: false
    default: ''
  enable-cache:
    description: 'Whether to enable Swatinem/rust-cache'
    required: false
    default: 'true'

runs:
  using: 'composite'
  steps:
    - uses: dtolnay/rust-toolchain@stable
      with:
        components: ${{ inputs.components }}
        targets: ${{ inputs.targets }}

    # v1 invalidates caches created before the workspace's dev-profile
    # fingerprints changed. Keep this fixed so every Rust job shares one cache
    # namespace and a future invalidation is a deliberate repository-wide edit.
    - if: ${{ inputs.enable-cache == 'true' }}
      uses: Swatinem/rust-cache@v2
      with:
        prefix-key: v1-rust

    - uses: extractions/setup-just@v4
