# ast-grep project config — repo-specific structural lint rules.
# Rules live in .ast-grep/rules/; run via `just comment-lint` (diff-scoped
# advisory) or `ast-grep scan` (whole-repo). See docs/CONTRIBUTING.md.
ruleDirs:
  - .ast-grep/rules
