{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SourceStatus",
  "description": "The STABLE `pixtuoid sources --json` wire contract the Raycast extension\nparses. Deliberately a flat DTO, NOT the internal `ConnectionRow` (whose\nshape is a UI concern free to change).",
  "type": "object",
  "properties": {
    "id": {
      "type": "string"
    },
    "display_name": {
      "type": "string"
    },
    "connected": {
      "type": "boolean"
    },
    "cli_present": {
      "type": "boolean"
    },
    "health": {
      "description": "A health/issue summary (install-broken / decode-drift), or `null` when n/a.",
      "type": [
        "string",
        "null"
      ]
    }
  },
  "additionalProperties": false,
  "required": [
    "id",
    "display_name",
    "connected",
    "cli_present"
  ]
}
